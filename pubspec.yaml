name: square_progress_indicator
description: You can create customizable square progress indicators like native flutter CircularProgressIndicator widget.
version: 0.0.4
homepage: https://github.com/amir14a/square_progress_indicator
repository: https://github.com/amir14a/square_progress_indicator
issue_tracker: https://github.com/amir14a/square_progress_indicator/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

screenshots:
  - description: 'Full example of SquareProgressIndicator'
    path: screenshots/screenshot1.gif